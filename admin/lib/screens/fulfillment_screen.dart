import 'package:flutter/material.dart';

class FulfillmentScreen extends StatelessWidget {
  const FulfillmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Fulfillment — coming in T079'),
    );
  }
}

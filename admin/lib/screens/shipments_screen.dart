import 'package:flutter/material.dart';

class ShipmentsScreen extends StatelessWidget {
  const ShipmentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Shipments — coming in T079'),
    );
  }
}

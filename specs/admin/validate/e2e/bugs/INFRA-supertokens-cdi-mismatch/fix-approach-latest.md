## Fix approach — INFRA-supertokens-cdi-mismatch

The previous fix agent correctly identified the CDI version mismatch but attempted to upgrade SuperTokens core to 11.1.0, which is not available from the SuperTokens download API. Instead, I downgraded `supertokens-node` from `^24.0.2` (requires CDI 5.4) to `^23.1.0` (requires CDI 5.3), which is compatible with SuperTokens core 11.0.x. I also reverted the setup script from `ST_CORE_VERSION="11.1.0"` to `"11.0.2"` (latest available patch). Verified: core 11.0.2 serves CDI 5.3, SDK 23.1.0 requires CDI 5.3, signup returns HTTP 200. All 210 unit tests pass, typecheck passes.

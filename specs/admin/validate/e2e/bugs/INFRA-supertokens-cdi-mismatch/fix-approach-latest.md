Bumped `ST_CORE_VERSION` from `"11.0.0"` to `"11.1.0"` in `scripts/setup-supertokens.sh` and deleted the `.dev/supertokens/` directory so the setup script re-downloads the correct version. SuperTokens core 11.0.0 only supports CDI up to 5.3, but `supertokens-node@24.0.2` requires CDI 5.4 exclusively. Core 11.1.0 ships CDI 5.4 support, resolving the version mismatch that caused all auth calls to return HTTP 500.

Added `{ id: "name", optional: true }` to the `formFields` array in `api/src/auth/supertokens.ts`. The Flutter customer app sends a `name` field during signup, but SuperTokens was configured with only `email` and `password` fields, causing it to reject requests with an unexpected field. Marking `name` as optional ensures backward compatibility while accepting the Flutter app's signup payload.

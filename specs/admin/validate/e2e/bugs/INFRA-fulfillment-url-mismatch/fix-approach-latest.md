Changed `admin/lib/providers/fulfillment_provider.dart` to use the correct API routes: line 9 `dio.get('/api/admin/fulfillment')` → `dio.get('/api/admin/fulfillment-tasks')` and line 20 `dio.get('/api/admin/fulfillment/$taskId')` → `dio.get('/api/admin/fulfillment-tasks/$taskId')`. The server registers only `/api/admin/fulfillment-tasks` and `/api/admin/fulfillment-tasks/:id`; the Flutter provider had the wrong path (missing `-tasks` suffix), causing every Fulfillment Queue screen load to return 404.

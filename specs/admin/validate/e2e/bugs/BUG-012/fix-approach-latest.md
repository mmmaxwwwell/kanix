Modified `findActiveProductsWithDetails()` in `api/src/db/queries/catalog.ts` to filter the public catalog to only products that have at least one product class membership. Test products created by integration tests never have class membership, while all real products (seeded by `seed.ts`) do. This is a zero-migration fix using existing table relationships. Also updated `public-catalog.integration.test.ts` to create a product class and membership for the test product so the test continues to pass.

Added the `id="cart-link"` attribute and `<span id="cart-count">` badge element to the cart link in the shop listing page nav (`products/index.astro`), matching the pattern used in `products/[slug].astro` and `kits.astro`. Added a `<script>` block that imports `getCart` from the cart library and calls `updateCartBadge()` on page load to fetch the cart and display the item count. The page has no add-to-cart functionality, so no `kanix:cart-update` event listener is needed.

Added `listAllOrders` function to `api/src/db/queries/order.ts` that selects all rows from the order table (id, orderNumber, email, status, paymentStatus, fulfillmentStatus, totalMinor, placedAt, createdAt) ordered by createdAt desc. Imported `listAllOrders` in `api/src/server.ts` and replaced the hardcoded stub `return { orders: [] }` in the GET /api/admin/orders handler with `return { orders: await listAllOrders(database.db) }`.

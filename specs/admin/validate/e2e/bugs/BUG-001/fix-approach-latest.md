Added `PUBLIC_API_URL=http://localhost:3000` to both `.env.example` (for documentation) and the root `.env` (for runtime). The existing `scripts/sync-env.sh` already propagates `PUBLIC_*` variables to `site/.env`, so once the source variable exists in root `.env`, it flows automatically to the Astro site. No code changes were needed — `fetchProducts()` in `products.ts` was already correctly reading `import.meta.env.PUBLIC_API_URL`; the variable simply wasn't defined.

Changed `admin/lib/providers/fulfillment_provider.dart` to use the correct server-side route URLs: `/api/admin/fulfillment` → `/api/admin/fulfillment-tasks` (list, line 9) and `/api/admin/fulfillment/$taskId` → `/api/admin/fulfillment-tasks/$taskId` (detail, line 20). The server only defines `/api/admin/fulfillment-tasks` and `/api/admin/fulfillment-tasks/:id`; the Flutter provider had the wrong path (missing the `-tasks` suffix), causing 404 on every load of the Fulfillment Queue screen.

BUG-001 was already fixed in a prior iteration (PUBLIC_API_URL added to .env.example and .env). Verified as FIXED by the verify agent — products page renders product cards with prices. The current fix iteration addresses the related INFRA-easypost-package-missing bug which adds `@easypost/api` to `api/package.json` so the checkout endpoint no longer crashes with a ModuleNotFoundError.

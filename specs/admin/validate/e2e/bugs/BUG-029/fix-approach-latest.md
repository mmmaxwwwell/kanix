Added `isEmailVerified` to the import from `./auth/index.js` in `api/src/server.ts`. In the GET /api/customer/me handler, after fetching the customer record, called `const emailVerified = await isEmailVerified(userId)` and returned `{ customer: { ...cust, emailVerified } }` so that the Flutter app receives the actual SuperTokens email verification state instead of defaulting to false.

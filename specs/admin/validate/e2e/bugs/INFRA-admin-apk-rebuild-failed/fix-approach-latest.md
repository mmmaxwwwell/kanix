The Gradle incremental build cache was reusing a pre-fix APK artifact (mtime 20:34 UTC) even though the source fix had been committed (dadef4a at 20:57 UTC). Running `flutter build apk --debug` without first cleaning the build directory caused Gradle to report exit 0 and "Built app-debug.apk" without actually recompiling — confirmed by the APK mtime remaining at 20:34 and `kernel_blob.bin` still containing `/api/admin/fulfillment` (old URL). This iteration ran `flutter clean` in `admin/` first (deleting `build/` and `.dart_tool/`), then `flutter build apk --debug` which completed with `✓ Built build/app/outputs/flutter-apk/app-debug.apk` after a full Gradle recompile (213.7s). The new APK's `kernel_blob.bin` contains `/api/admin/fulfillment-tasks` (correct URL). The APK was installed via `adb uninstall com.kanix.kanix_admin && adb install` (debug signing key mismatch required uninstall before install).

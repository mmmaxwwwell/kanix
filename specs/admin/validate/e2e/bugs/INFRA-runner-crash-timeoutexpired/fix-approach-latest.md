The crash occurs in `parallel_runner.py` at line 14822 where `subprocess.run(adb logcat -d -t 200 '*:E', timeout=15)` raises `subprocess.TimeoutExpired` when the emulator is slow to respond, and the exception propagates uncaught, killing the E2E inner loop. The fix is to wrap the `subprocess.run` call in a `try/except subprocess.TimeoutExpired` block and fall back to `"(logcat timed out — emulator may be unresponsive)"`, also increasing the timeout from 15 to 30 seconds to give slow emulators more headroom. The fix cannot be applied in this agent run because `/home/max/git/agent-framework/.claude/skills/spec-kit` is mounted read-only (`ro,nosuid,nodev,relatime`). See the out-of-scope claim in the fix agent's final message for the proposed diff.

Added `@easypost/api` (^8.0.0) to `api/package.json` dependencies and ran `pnpm install`. The package was used via dynamic import in `api/src/services/shipping-adapter.ts:224` (`await import('@easypost/api')`) but was never declared as a dependency, causing `ModuleNotFoundError` at runtime whenever the real EasyPost adapter was selected (i.e., when `EASYPOST_API_KEY` is set to a real key). Verified the installed package (v8.8.0) exports a default constructor matching the code's usage pattern. The running API server needs a restart to pick up the new dependency.

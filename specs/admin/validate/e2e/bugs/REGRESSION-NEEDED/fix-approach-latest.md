No standalone code fix — REGRESSION-NEEDED is a tracking item requiring a full E2E device run. BUG-031/032/033 were fixed in iter-2/3; BUG-034 (clawback display) and BUG-035 (donation toggle/donationEnabled) are now fixed in this iteration. The verify.sh exits 2 (inconclusive) to signal that a verify agent with an Android emulator must re-run the contributor dashboard happy path steps 4-11 to validate all fixes together and produce the regression spec.

BUG-002 (no Support tab) and BUG-003 (photo upload stub) are now fixed. Wrote `customer/integration_test/T104b_test.dart` as the Patrol regression test for the warranty claim navigation flow. The test covers: (1) auth guard redirects unauthenticated users to login; (2) login form validation (email/password required) — ensures auth boundary enforces state transitions; (3) AppShell NavigationBar has 5 destinations with Support at index 2 between Cart and Orders; (4) `WarrantyClaim.isWithinWarranty` model logic — verifies warranty validity is computed from `warrantyExpiresAt` (true for future date, false for past, false for null). Tests 1–3 run against the real app via `app.main()`; test 3 also pumps a minimal NavigationBar to assert the Support tab structure. Test 4 is a pure model-layer behavioral test requiring no network.

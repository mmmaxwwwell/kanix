Added the missing "Kits" and "Cart" navigation links to the checkout page's `<nav>` in `site/src/pages/checkout.astro`. The checkout page had only Home and Shop links, while other commerce pages (products index, product detail, kits) included Kits and Cart links. Added the two missing `<a>` tags with the same styling, marking "Cart" as the active link (using `text-white font-medium`) since the checkout page is the cart page.

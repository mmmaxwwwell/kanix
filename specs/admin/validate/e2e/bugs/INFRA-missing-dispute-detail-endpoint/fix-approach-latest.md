Same fix as BUG-024. Added `getDisputeDetail` DB query function to
`api/src/db/queries/evidence.ts` and `GET /api/admin/disputes/:id` handler to
`api/src/server.ts`. See BUG-024/fix-approach-latest.md for full details.

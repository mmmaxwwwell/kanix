Fixed the API response shape mismatch between `GET /api/contributors/dashboard` and Flutter models. Two changes: (1) `contributor_provider.dart` now unwraps `response.data['dashboard']` before passing to `ContributorDashboardData.fromJson`. (2) Updated `customer/lib/models/contributor.dart` — `ContributorDesign.fromJson` now reads `productTitle`→`name`, `productSlug`→`slug`, `salesCount`→`totalSales`; `ContributorMilestone.fromJson` now reads `milestoneType` (humanized to name), `notes`→`description`, derives `targetSales` from type constants, `achieved` from `reachedAt != null`; `ContributorPayout.fromJson` reads `amountMinor`→`amountCents`, `completedAt??initiatedAt`→`paidAt`; `ContributorDashboardData.fromJson` computes `totalDesigns`/`totalSales` from designs array and `totalRoyaltyAccruedCents`/`totalPaidOutCents` from `royaltySummary` object. API unchanged.

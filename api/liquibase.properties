# Liquibase configuration for Kanix API
# Database connection is configured via DATABASE_URL environment variable.
# The PostgreSQL JDBC driver is provided by the Nix devshell (LIQUIBASE_CLASSPATH).

changeLogFile=migrations/changelog-master.xml
url=jdbc:postgresql://localhost:5432/kanix
username=kanix
password=kanix

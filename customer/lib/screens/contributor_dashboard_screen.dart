import 'package:flutter/material.dart';

class ContributorDashboardScreen extends StatelessWidget {
  const ContributorDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Contributor Dashboard'),
    );
  }
}
